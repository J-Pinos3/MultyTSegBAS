<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Para fotos de cámara - Ruta específica que estás usando -->
    <external-files-path
        name="visitor_photos"
        path="Pictures/visitor_photos" />

    <!-- Para compatibilidad con otras rutas -->
    <external-files-path
        name="my_images"
        path="Pictures" />

    <!-- Cache para archivos temporales -->
    <cache-path
        name="cache"
        path="." />

    <!-- Files directory general -->
    <files-path
        name="files"
        path="." />

    <!-- External Storage general -->
    <external-path
        name="external"
        path="." />
</paths>